После клонирования проекта создайте файл .env взяв за пример .env.example, и настройте его.

Затем выполните команду:

```commandline
docker compose up --build -d
```

Теперь приложение готово к работе.

